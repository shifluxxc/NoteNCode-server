import { mutation } from "./mutation";
import { Query } from "./query";
import { typedef } from "./typedef";
import { resolvers } from "./resolver";


export const notes = { mutation, Query, typedef, resolvers }; 